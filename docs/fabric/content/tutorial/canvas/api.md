### 属性

| 属性名                 | 说明                                        | 默认值                                                      |
|---------------------|-------------------------------------------|----------------------------------------------------------|
| `allowTouchScrolling` | 表示当使用触摸屏并在画布上拖动时，浏览器是否可以滚动。 | 

### 方法
